[package]
name = "overlay-x11"
version = "0.1.0"
edition = "2021"

[dependencies]
x11rb = { version = "0.13", features = ["all-extensions"] }
libc = "0.2"
png = "0.17"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
